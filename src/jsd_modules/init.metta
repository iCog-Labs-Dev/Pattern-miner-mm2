(exec (0)
    (,  (exec (emp-tv $pri-emp $name-emp) $src-of-emp $sink-of-emp)
        (exec (eq-prob $pri-eq $name-eq) $src-of-eq $sink-of-eq)
        (exec (surp $pri-surp cleanup) $src-of-cl $sink-of-cl)
    )
    (O
        (- (exec (eq-prob $pri-eq $name-eq) $src-of-eq $sink-of-eq))
        (- (exec (emp-tv $pri-emp $name-emp) $src-of-emp $sink-of-emp))
        (- (exec (surp $pri-surp cleanup) $src-of-cl $sink-of-cl))
        (+ (exec (surp 124 transition-to-emp) 
            (,) 
            (O (+ (exec (emp-tv $pri-emp $name-emp) $src-of-emp $sink-of-emp)))))
                (+ (exec (emp-tv 118 transition-to-eq-prob) (,) 
                    (, 
                        (exec (eq-prob $pri-eq $name-eq) $src-of-eq $sink-of-eq)
                        (exec (eq-prob $pri-surp cleanup) $src-of-cl $sink-of-cl)
                    )))
            )
)